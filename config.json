{
    "presets": {
        "localhost": {
            "host": "127.0.0.1",
            "port": 3000,
            "routes": {
                "login": "/auth/login",
                "register": "/auth/register",
                "user": "/user",
                "sendMessage": "/channels/{}"
            }
        }
    },

    "defaults": {
        "host": null,
        "port": 3000,
        "routes": {
            "login": "/auth/login",
            "register": "/auth/register",
            "user": "/user",
            "sendMessage": "/channels/{}"
        }
    }
}